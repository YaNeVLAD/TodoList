package com.example.todoList.presentation.builder

import com.example.todoList.presentation.common.BaseViewModel

class TaskBuilderViewModel : BaseViewModel<TaskBuilderState, Any>(
    initialState = TaskBuilderState()
) {
}